# Aliases.
alias unseen='diff <(cat ~/films-hidden.txt ~/films-seen.txt|sort) <(ls -1 --color=never) | grep -oP "(?<=^> ).*" | grep -v ^\@eaDir | grep -v "\.\(zip\|srt\)$"'

# Paths.
export PYTHONPATH=$PYTHONPATH:/opt/whatlastgenre/
export BEETSDIR=/media/droppie/libraries/music/.config/beets
